
function loadImage(image_url) {
	var img = new Image();
	$(img).load(function() {
		$(".image-desc-wrapper").html(img);
		$(img).fadeIn(300);
	}).attr({src: image_url, id: "largeImage"});
}
loadImage("<%= @new_image.image.url %>");