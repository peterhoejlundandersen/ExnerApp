<% img_cats.each_with_index do |img_nav_cat, index| %>
	<%= content_tag :div, img_nav_cat.name, class: "nav-link sortable-image-item text-center #{"active-cat" if index == 0}", data: { id: img_nav_cat.id, type: img_nav_cat.class.to_s } %>
<% end %>
