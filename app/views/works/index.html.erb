<%= render partial: 'about/pagination',
  locals: { prev: { title: @prev[:title], path: @prev[:path]},
              next_w: { title: @next[:title], path: @next[:path] } } unless @pagination.nil? %>

<input type="hidden" data-category-id="<%= @category.friendly_id %>" id="categoryId">
<works-index></works-index>
<%= javascript_pack_tag 'works_index' %>

