<% images.each do |image| %>
	<%= content_tag :div, (image_tag image.image.thumb.url), class: "thumb-image col-lg-2 col-md-4 col-sm-6 sortable-image-item", data: {id: image.id, type: image.class.to_s} %>
<% end %>
