<div class="row">
<% @categories.each do |cat| %>
		<div class="col-lg-4">
	<%= link_to vaerker_path(cat.id) do %>
			<%= image_tag cat.image if cat.image.present? %> 
			<h4 class="text-center"><%= cat.name %></h4>
	<% end %>
		</div>

<% end %>
</div>